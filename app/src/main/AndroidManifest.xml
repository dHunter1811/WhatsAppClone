<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.whatsappclone">
    <!-- === TAMBAHKAN IZIN DI BAWAH INI === -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WhatsAppClone"
        tools:targetApi="31">
        <activity
            android:name=".ui.GroupChatActivity"
            android:exported="false" />
        <activity
            android:name=".ui.CreateGroupInfoActivity"
            android:exported="false" />
        <activity
            android:name=".ui.SelectMembersActivity"
            android:exported="false" />
        <activity
            android:name=".ui.IncomingCallActivity"
            android:exported="false" />
        <activity
            android:name=".ui.CallActivity"
            android:exported="false" />
        <activity
            android:name=".ui.AddStatusActivity"
            android:exported="false" />
        <activity
            android:name=".ui.StatusViewerActivity"
            android:exported="false" /> <!-- LoginActivity sebagai entry point -->
        <activity
            android:name=".ui.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Deklarasi ChatActivity -->
        <activity
            android:name=".ui.ChatActivity"
            android:exported="true" /> <!-- MainActivity -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="false" />
    </application>

</manifest>